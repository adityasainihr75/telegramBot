src/
├── bot/               # Bot logic
│   ├── commands.js    # /start, /securelink handlers
├── models/            # Database models
│   └── Link.js
├── routes/            # Express routes
│   └── api.js         # /api/resolve endpoint
├── utils/             # Helpers
│   └── logger.js      # Custom logger
└── app.js             # Main entry

flow:
User → /securelink → Bot → Store in DB → Generate Masked Link
                      ↓
User Clicks Link → Bot → Mini-App → API → DB → Redirect (tg://)

env creds formats:
//APP_NAME is the web app name that you have linked with your bot in botfather 
APP_NAME = *******
BOT_OWNER_ID = *******
BOT_TOKEN = *******
BOT_USERNAME = *******
MONGODB_URI = *******
port = *******
REDIS_URI = *******
Tester_ID = 
